import { TemplateFn, CHANGE_TYPE } from '../../../../lib/webcomponents';
import { PasswordCreate } from './password-create';

export const PasswordCreateCSS = new TemplateFn<PasswordCreate>((html) => {
		return html`<style>
			
		</style>`
	}, CHANGE_TYPE.PROP);
