import { TemplateFn, CHANGE_TYPE } from '../../../lib/webcomponents';
import { FloatingActionButton } from './floating-action-button';

export const FloatingActionButtonCSS = new TemplateFn<FloatingActionButton>((html) => {
		return html`<style>
			
		</style>`
	}, CHANGE_TYPE.PROP);
