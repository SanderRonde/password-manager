language: node_js
node_js: "10"
cache: yarn

branches:
  only:
  - master
stages:
- test
  
install:
- yarn --ignore-engines
- npm install -g gulp
script:
- yarn compile
- yarn test
notifications:
  email:
    on_success: change
    on_failure: change